import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    primary: {
      main: "#4B1248", // Your primary color
    },
    secondary: {
      main: "#F0C27B", // Your secondary color
    },
  },
});

export default theme;
